# 文档定位

保持与大模型沟通时必须对齐的定位、目标、边界。

## 边界

- 做：主链、记忆、技能路由、工具运行时的稳定迭代。
- 不做：让扩展能力反向主导架构与默认流程。

## MCP 定位

- 立场：MCP 维持可支持，但默认弱化存在感。
- 默认：不自动加载 MCP，仅在显式启用或专项场景接入。
- 要求：MCP 相关改动不得拖慢默认回归，不得破坏主链稳定。

## 测试策略

- 默认命令：python tests/run_tests.py（快速回归）。
- 全量命令：python tests/run_tests.py --all（含慢集成）。
